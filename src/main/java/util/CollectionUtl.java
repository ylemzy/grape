package util;

/**
 * Created by huangzebin on 2017/2/24.
 */
public class CollectionUtl {


/*    <K, T> Map<K, T> toMap(Iterable<T> datas){



        List<UserOrderBaseDocument> ss = null;
        return ss.stream().collect(Collectors.toMap(UserOrderBaseDocument::getId, p -> ));


        Map<K, T> map = new HashMap<K, T>();

        datas.forEach(d ->{
            map.put()
        });
        return map;
    }*/
}
